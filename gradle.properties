kotlin.code.style=official

# Multi-module performance (diary 053). Parallel task execution + build cache +
# configuration cache. The first is always-win for multi-module; the others pay off
# on incremental builds and warm-daemon loops.
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configuration-cache=true
